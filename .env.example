# Copia este archivo a .env.local (no se sube a git) y pon tus valores reales,
# de Supabase → tu proyecto → Settings → API.
# Sin esto la app funciona igual, solo local, sin sincronizar entre dispositivos.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
